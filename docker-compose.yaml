version: "2"
services:
  microservice1:
    image: microservice1
    restart: on-failure
    tty: true
    hostname: microservice1
    mem_limit: 1024M
    mem_reservation: 512M
    volumes:
      - /etc/hosts:/etc/hosts
      - /var/run/docker.sock:/var/run/docker.sock
    networks:
      - Network_app

  microservice2:
    image: microservice2
    restart: on-failure
    tty: true
    hostname: microservice2
    mem_limit: 4096M
    mem_reservation: 2048M
    volumes:
      - /etc/hosts:/etc/hosts
      - /var/run/docker.sock:/var/run/docker.sock
    networks:
      - Network_app

  grafana:
    ports:
     - 3000:3000
    image: grafana/grafana
    container_name: grafana
    networks:
      - Network_app

  prometheus:
    container_name: prometheus
    image: prom/prometheus
    ports:
      - 9090:9090
    volumes:
      - /Users/ritgirdh/Desktop/codebase/study/microservices-prometheus-demo/prometheus/:/etc/prometheus/
    command: "--config.file=/etc/prometheus/prometheus.yml --web.enable-lifecycle"
    networks:
      - Network_app

networks:
  Network_app: